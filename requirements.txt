fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
# passlib 1.7.4 (its last release) is incompatible with bcrypt>=4.1 and
# crashes on the very first password hash. Pin to the last known-good
# bcrypt release until passlib gets a fix or Butler moves off it.
bcrypt==4.0.1
yt-dlp>=2024.4.9
httpx>=0.27
requests>=2.31
pydantic>=2.6
python-multipart>=0.0.9
